import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
    colors: {
        primary: "#040006",
        customBlue: "#052FC2",
        navLinkColor: "#AFAFB1"
    }
})

export default theme